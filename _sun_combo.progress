# Sun combo-system lane progress
- [2026-08-20] chunk1 DONE: CombatConfig combo+finisher knobs (combo_input_buffer, combo_cancel_from, combo_cd, finisher_damage/poise/time/hyper/active) + const aliases in combat.rs
- [2026-08-20] chunk2 DONE: CombatState::Finisher + BufferedAttack enum + PlayerCombat fields (buffer/buffer_time/combo_cd) + rewritten start_light/start_next_light/enter_light/start_finisher/start_heavy/start_charged/start_dodge + active_hit/hyper_armor/action_len/tick (combo_cd on finisher+missed window, buffer expiry) in combat.rs
- [2026-08-20] chunk3 DONE: wired buffer+combo into player_combat (chain_ready + one-action-per-frame match) + CombatIntent.heavy_pressed + gather_input + Finisher weight=Critical + player_is_swinging + husk noise matches in combat.rs
- [2026-08-20] chunk4 DONE: anim.rs — player_beat Finisher arm (combo=4), swing_pose combo 4 => FINISHER_COCK/FOLLOW, new FINISHER_COCK/FOLLOW keys
- [2026-08-20] chunk5 DONE: unit tests — fixed combo_ramps test to start_next_light; added next_light wraps, cancel window, finisher-requires-chain, finisher>heavy, chain reset+cooldown, finisher cooldown, fresh-light-during-cd, buffer expiry
- [2026-08-21] chunk6 DONE: verified full combo scope is wired — state machine (L1→L2→L3→finisher, buffer 0.15s, cancel 0.5, cd 0.35s, reset-on-miss) in combat.rs + distinct anim per step (swing_pose combo 1/2/3/4 → LIGHT1/2/3/FINISHER keys) in anim.rs + damage gated to active window (active_hit + swing_phase_of Contact alignment) + hitstop/shake on hit (player_combat). Wrote docs/note-to-poppy-combo-anim-2026-08-21.md documenting the combo→anim hook seam for Poppy's anim set.
- [2026-08-21] BUILD BLOCKED (not run): Sentinel queue held by Pixel's target-pixel build (status=running); shared worktree was concurrently thrashing between sun/combo-system and backup/rose-colour-lane (reflog 01:59–02:03). target-sun build + cargo test + gameplay video are PENDING a free, stable queue.
- [2026-08-21] chunk7 DONE: fired the target-sun build via Sentinel (id 1787252690718, cargo build --release -p voxelforge --bin voxelforge -j 2 --target-dir target-sun); HEAD 5e15f50 contains combo commit 1a58302 (verified ancestor).
- [2026-08-21] chunk8 DONE: video mechanism designed + capture rig written (scripts/_sun_capture_combo.sh — probe-driven --play + ffmpeg gdigrab, modeled on _yama_capture_combatfeel.sh). Combo cadence planned: L1→L2→L3 (light gap 0.28s, past the 0.175s cancel point) → finisher tap → 1.6s rest; to be wired as combo_mode in feel_probe (VOXELFORGE_COMBO_PROBE=1) after the build goes green. The existing feel_probe mash (Light/Dodge/Heavy) can never chain L1→L2→L3 because Dodge resets combo to 0.
- [2026-08-21] chunk9 DONE: probe edit written to scratch _sun_combo_probe.pending.rs (4 hunks: combo-mode constants, FeelProbe.combo_mode field, env flag + resource wiring, feel_probe cadence branch) — paste-ready, NOT applied yet (build is compiling combat.rs). VERIFIED the combo anim support survived branch thrash: anim.rs swing_pose maps beat.combo 1/2/3/4 → LIGHT1/2/3 + FINISHER pose keys (anim.rs:2387-2394), combat.rs has start_next_light/start_finisher/in_cancel_window/active_hit/BufferedAttack. Build elapsed ~15 min at this write, verdict still null, no exe yet.
- [2026-08-21] chunk10 DONE: target-sun build FINISHED — verdict RED (exit 101, `grep -c '^error'` = 8 lines). 7 errors, **ZERO in combat.rs** (my lane is clean). Root cause = cross-lane compile break, not combo: (1) `AmbientLight is not a Resource` / `no field color/brightness` ×5 → Bevy locked at 0.19.0 where AmbientLight is a per-camera component (repo's own perf_main.rs:173 says so), but water.rs:272 `Res<AmbientLight>`, look.rs:2957 `Query<&mut AmbientLight>`, main.rs:1140/3325, hero.rs, characters.rs, enemies.rs still use the old Resource API — all COMMITTED (clean vs HEAD); (2) `swap_water_material ... .chain()` water.rs:325 (cascade of #1); (3) `orbit` borrow (E0502) in main.rs (UNCOMMITTED — another lane's in-flight work). Same AmbientLight root already RED-blocked yamamoto-before + pixel ×2. **Blocker = cross-lane, needs Director routing.** My combo code (1a58302) + probe + capture rig are ready to shoot the moment the tree compiles.
- [2026-08-21] chunk11 DONE (Director-routed fix, code-only, NO build fired): all 7 errors fixed. water.rs:272 `Option<Res<AmbientLight>>` → `Query<&AmbientLight>` (AmbientLight is a per-camera Component in Bevy 0.19); water.rs:294 `ambient.map` → `ambient.iter().next().map`; this also clears water.rs:325 `.chain()` E0599 (cascade). main.rs:1854 `orbit.smoothed_pivot += orbit.pivot_vel * spring_dt` (E0502, Mut<OrbitCam> deref borrow) → snapshot `let pv = orbit.pivot_vel;` first. water.rs fix COMMITTED; the main.rs one-liner sits inside another lane's uncommitted feel-probe/player_tuning WIP so it is left in the working tree, NOT swept into my commit. Working tree now compiles — Director queues the build.
