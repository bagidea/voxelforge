# Voxelforge — วิธีเล่น (อ่าน 2 นาที แล้วเล่นได้เลย)

## เริ่มเล่น

ดับเบิลคลิก **`PLAY.bat`** ที่โฟลเดอร์นี้ — เข้าเกมตรงเข้าฉากเล่นจริง ไม่ผ่าน editor
(หน้าต่าง console จะปิดเองทันที ไม่ค้าง)

**พอเข้าเกมแล้วให้คลิกซ้าย 1 ครั้งก่อน** เพื่อล็อกเมาส์เข้าหน้าต่าง — ถ้ายังไม่คลิก เมาส์จะยังไม่หมุนกล้อง
บนแถบ HUD บนสุดจะเขียนว่า `(cursor: click to lock)` เตือนอยู่

---

## ปุ่มควบคุมทั้งหมด

### เดิน / กล้อง

| ปุ่ม | ทำอะไร |
|---|---|
| `W` `A` `S` `D` | เดิน (ทิศอิงกล้อง — W = ไปทางที่กล้องหันอยู่) |
| เลื่อนเมาส์ | หมุนกล้อง (ต้องล็อกเมาส์ก่อน) |
| `Ctrl` ซ้าย | วิ่ง (เร็วขึ้นจาก 6 → 10) |
| `Space` | กระโดด (เฉพาะตอนเท้าติดพื้น) |
| คลิกซ้าย | ล็อกเมาส์เข้าหน้าต่าง |
| `Esc` | ปล่อยเมาส์ / เปิด-ปิดเมนู Settings |
| `F` | สลับ เดินติดพื้น ↔ บินทะลุ (noclip) — ตอนบิน `Space` = ขึ้น, `Shift` ซ้าย = ลง |

### ต่อสู้

| ปุ่ม | ทำอะไร |
|---|---|
| คลิกซ้าย **หรือ** `X` | ฟันเบา |
| `C` (กดค้าง) | ฟันหนัก |
| คลิกขวา (กดค้าง) | ตั้งการ์ด / บล็อก |
| `V` | **parry** (สวนจังหวะ) |
| `Space` | **dodge** (กลิ้งหลบ) |
| `R` | ล็อกเป้า เปิด/ปิด |
| `Q` | สลับเป้าที่ล็อกอยู่ |

### คุย NPC

| ปุ่ม | ทำอะไร |
|---|---|
| `E` | คุยกับ NPC — เดินเข้าไปใกล้แล้วจะมีข้อความ `[E] Talk to <ชื่อ>` ขึ้นมาก่อน |
| `Space` หรือ `Enter` | ข้อความถัดไป |
| `Esc` | ปิดบทสนทนา |

### เมนู / ออกเกม

| ปุ่ม | ทำอะไร |
|---|---|
| `Esc` | เปิด/ปิดเมนู Settings (มีปุ่มตั้งค่าคีย์ใหม่ + เสียง) |
| `Alt`+`F4` หรือกดกากบาทหน้าต่าง | **ออกเกม** — ยังไม่มีปุ่ม Quit ในเมนู |

### ปุ่มโหมด build (ยังเปิดอยู่ระหว่างเล่น)

`Tab` สลับ PLAY ↔ EDIT · `G` fill · `F5` เซฟโลก · `F9` โหลดโลก · `1`–`4` เลือกบล็อก

> ⚠️ **ปุ่มที่ทับกันจริง — รู้ไว้จะได้ไม่งง**
> - **คลิกซ้าย** = ล็อกเมาส์ + ฟันเบา + ทุบบล็อก (HUD ขึ้น `L=break`)
> - **คลิกขวา** = ตั้งการ์ด + วางบล็อก (`R=place[stone]`)
> - **`Space`** = กระโดด + dodge + กดข้อความถัดไป
> - **`Esc`** = ปิดบทสนทนา / เปิดเมนู / ปล่อยเมาส์
> - **`R`** = ล็อกเป้า แต่ถ้ายืนอยู่ในระยะเควสที่สั่งให้วางบล็อก เควสจะกินปุ่มไปก่อน

---

## ⚔️ อ่านก่อนกด `W`: มีศัตรูยืนรออยู่ 7 บล็อกตรงหน้า

**เหตุการณ์แรกสุดของเกมคือการต่อสู้ ไม่ใช่การเดินเล่น**

ทุกครั้งที่เข้าเกมด้วย `PLAY.bat` เกมจะสปอว์น **Guard Husk 1 ตัว ตรงหน้าคุณพอดี**
คุณเกิดที่ `(32.5, 32.5)` มันยืนที่ `(32.0, 25.0)` — **ห่างแค่ 7.5 บล็อก บนถนนเส้นเดียวกับที่คุณต้องเดิน**
มันเห็นคุณตั้งแต่เฟรมแรก (ระยะ aggro 12 บล็อก) และจะเริ่มวิ่งเข้าใส่จริงเมื่อคุณเข้าใกล้กว่า 6 บล็อก
คือ **เดินไปแค่ 1.5 บล็อก ≈ ไม่ถึงครึ่งวินาทีหลังกด `W`**

**วิธีรับมือ (เลือกอย่างใดอย่างหนึ่ง):**

- **สู้** — เลือดมัน 80 มันฟันได้ตอนเข้าระยะ 2 บล็อก
  จังหวะที่ปลอดภัยที่สุดคือ **กดขวาค้างตั้งการ์ดไว้ก่อน** → พอมันเงื้อดาบกด `V` parry → แล้วรัวคลิกซ้าย
  ถ้ากลัวพลาด กด `Space` กลิ้งหลบออกด้านข้างแล้วค่อยเข้าฟันหลัง
- **หนี** — มันเลิกไล่ทันทีที่คุณห่างเกิน 6 บล็อก ถ้าอ้อมออกทางข้างแล้ววิ่ง (`Ctrl` ซ้าย) ผ่านไปเลยก็ได้
  มันเดินลาดตระเวนอยู่แถวนั้น ±6 บล็อกตามแกน X เท่านั้น ไม่ตามคุณไปถึงประตู

> ⚠️ ตายแล้วไม่ได้เริ่มใหม่แบบสบายๆ นะคะ — คุณฟื้นที่กองไฟ `(32.5, 29.5)`
> ซึ่ง **ห่างมันแค่ 4.5 บล็อก = อยู่ในระยะที่มันไล่ทันที** และเกม **ชุบเลือดมันเต็ม 80 กลับที่เดิม**
> (`RESPAWN at campfire ... enemies_reset=1`) ตีมันไว้เท่าไหร่หายหมด สู้ทีเดียวให้จบ หรืออ้อมหนีไปเลย

---

## เป้าหมายแรก: เดินตรงไปข้างหน้า

**ตอนเกิดคุณหันหน้าถูกทิศอยู่แล้ว — จัดการ Husk ข้างบนเสร็จ ก็กด `W` ค้างไว้อย่างเดียวพอ**

เกมสปอว์นคุณที่กลางหมู่บ้าน Edhari ยืนหันหน้าไปทางทิศเหนือ (`-Z`) โดยตั้งใจ
ทุกเป้าหมายของ Act 1 เรียงอยู่บนเส้นตรงเส้นเดียวกันข้างหน้าคุณ

### เซฟปัจจุบันเปิดมาที่เควส 2 — “A Voice in the Stone”

มุมขวาบนจะขึ้นชื่อเควสให้เห็น เป้าหมายคือ:

1. **เดินตรงไปข้างหน้าเรื่อยๆ ~24 บล็อก** จนเข้าเขตลานประตูใหญ่ `gate_square`
   — เควสนี้เป็นแบบ *เข้าเขต* ไม่ใช่ *เข้าใกล้จุด*: มันนับผ่านเมื่อตัวคุณอยู่ในกรอบ
   **x 27–37 · z 3–8** เท่านั้น เดินตรงมาจาก z32.5 คุณจะเข้ากรอบตอน z=8 พอดี
   (ยืนใกล้ๆ นอกกรอบไม่นับ ต้องเข้าไปในกรอบจริง)
2. ถึงแล้วจะมีเสียงเรียกจาก **Elder Maren** ที่พูดผ่านรอยแตกของประตูที่ถูกผนึกไว้ — กด `E` คุย แล้ว `Space` รัวไปเรื่อยๆ จนจบ

เควสถัดไป (`The Gatekeeper`) จะให้เลี้ยว **ขวา ไปทางตะวันออก** หาด่านยาม (`guard_post_east`, x 48–56 · z 4–12)
ที่นั่นจะมี **Garren the Husk** โผล่มาที่ `(53.5, 8.0)` — โผล่ตอนเควส 3 เริ่มเท่านั้น ไปดูก่อนหน้านั้นจะไม่เจอตัว
เควสสั่งไว้ตรงๆ ว่า *"Watch it before you fight it"* ดูมันสักพักก่อนค่อยเข้าไปฟัน (ยืนในระยะ 12 บล็อกก็นับว่าดูแล้ว)

### อยากเริ่มใหม่ตั้งแต่ต้นเรื่อง

ลบไฟล์ **`quest_save.json`** ที่โฟลเดอร์นี้ทิ้ง แล้วเปิดเกมใหม่ —
จะได้เควส 1 “Embers”: ตื่นมาในเพิงที่พังแล้ว มี **กองไฟกองเดียวอยู่ข้างหน้าห่างไป 3 บล็อก**
เดินไปยืนใกล้ๆ ก็จบเควสแรก (กองไฟเป็นจุดฟื้นด้วย)

> Husk ตัวแรกยังอยู่ที่เดิมนะคะ — กองไฟอยู่ข้างหน้า 3 บล็อก มันอยู่ 7.5 บล็อก **บนเส้นเดียวกัน**
> พอถึงกองไฟคุณจะเหลือระยะห่างมันแค่ 4.5 บล็อก = มันไล่แล้ว
> เพราะฉะนั้นเควสแรกจะจบพร้อมกับที่ต้องรับมือมันไปด้วย เตรียมนิ้วไว้ที่คลิกขวา/`V`

---

*ปุ่มทั้งหมดข้างบนอ่านมาจากโค้ดจริง: `client/src/main.rs` (เดิน/กล้อง/บิน) · `client/src/combat.rs::gather_input` (ต่อสู้) · `client/src/quest.rs` + `client/src/dialogue_ui.rs` (คุย NPC) · `client/src/settings_menu.rs` (เมนู)*

*ตัวเลขในหมวดศัตรู/เป้าหมายอ่านจาก: `client/src/scene.rs:473-487` (สปอว์น Husk ที่ `sz - 7.0` ทุกครั้งที่โหลดแมป) · `client/src/combat.rs:208/235/248/249` (`melee_range 2.0`, `hp_husk 80`, `husk_aggro_range 12.0`, `husk_leash 6.0`) · `client/src/quest.rs::check_area_triggers` (`reach_zone` ใช้ bounds ของ region ไม่ใช่ `radius`) · `client/src/quest.rs:757` (`GARREN_POS`) · bounds/เควสจาก `assets/story/act1.json`*
