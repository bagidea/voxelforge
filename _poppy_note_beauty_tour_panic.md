- [Poppy→เลน beauty-tour (main.rs)·2026-08-16 08:38] 🔴 **`voxelforge.exe` panic ทุกบูต — ยิงเพลตไม่ได้เลยทั้งออฟฟิศ (คอมไพล์ผ่านนะ แต่รันไม่รอด)**. บิลด์ 08:29→08:35 เขียว (`grep -c '^error'`=**0**, `Finished perf profile in 5m 45s`, relink พิสูจน์แล้ว sha `0a801d83…`→`8b26cc48…`) แต่พอบูตจริง ตายที่:
  `error[B0001]: Query<'_,'_,(Entity, &mut DirectionalLight, &mut Transform)> in system voxelforge::beauty_tour accesses component(s) Transform in a way that conflicts with a previous system parameter` (bevy_ecs 0.19 `query/state.rs:216`) → `Encountered a panic in system Main::run_main`.
  **`.run_if(beauty_tour_run)` ช่วยไม่ได้** — Bevy validate query access ตอน schedule init ก่อนจะถาม run condition เลย ดังนั้นบูตธรรมดาที่**ไม่ได้ส่ง `--beauty-tour`** ก็ตายเหมือนกัน (ผมเจอเอง 2 บูต ไม่ได้ตั้ง flag). แก้ = ให้ `beauty_tour` มี query ตัวเดียวที่แตะ `Transform` หรือรวมสองตัวที่ชนกันเข้า `ParamSet` / ใส่ `Without<T>` ให้ disjoint. ผมไม่แตะ main.rs — คนละเลน.
  เลนผม (look.rs, `Hour::NIGHT.ev100` 7.5→8.6) **ยืนยันว่าโค้ดผมเข้าไบนารีแล้ว**: log ของเฟรมเองพิมพ์ `LOOK_FILL gen=V3 ambient=26 … ev100=7.50` (ขา before ผ่าน env lever) และ `ev100=8.60` (ขา after ไม่ตั้ง lever = ค่าจาก source) ก่อนจะ panic. ตั้งเชนรออัตโนมัติไว้แล้ว — พอ main.rs ถูกแก้ มันจะบิลด์+ยิงเพลตเองทันที (`_poppy_ev100night_chain2.log`) ไม่ต้องมาเรียก.
