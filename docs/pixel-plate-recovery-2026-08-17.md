# กู้เพลตความสวย 64 ใบ ที่ `git clean -fd` ลบไป — ยิงใหม่ครบ 64/64 (2026-08-17)

**Lane:** Pixel / Flamingo · **Branch:** `poppy/native-only` · **ยังไม่ push**
**สถานะ:** เพลตกลับมาครบทุกใบตาม manifest · คำตัดสินของบันไดเดิม **ยังยืน** · แต่ตัวเลข **ขยับจริง** และไม่ได้กลบไว้

เอกสารนี้ต่อจาก `docs/RECOVERY-2026-08-16.md` §3 ซึ่งรับรองว่า "อินพุตสำหรับยิงใหม่รอดครบ"
แล้วเว้นการยิงจริงไว้ให้เลนที่ถือลุค. นี่คือการยิงนั้น.

---

## 0. ของที่ส่ง

| ของ | path | นับได้ |
|---|---|---|
| เพลตบันได | `docs/assets/look/beauty/ladder/` | **52** (4 โพส × 13 ขั้น) |
| เพลต probe | `docs/assets/look/beauty/probe/` | **9** (8 โพส + `_contact.png`) |
| before/after 3 แผ่น | `docs/assets/look/beauty/ba-{atmosoff,overfog,inert}.png` | **3** |
| | | **รวม 64/64** ตรงกับ manifest ของ `21e28a8` |
| ตารางกริด **ของเดิม 08-16** (กู้จาก `_grid.json`) | `docs/assets/look/beauty/_grid.md` | 44 เพลต |
| ตารางกริดใหม่ ทุกพิกเซล | `docs/assets/look/beauty/_grid_reshoot.md` | 52 เพลต |
| ตารางกริดใหม่ เฉพาะพิกเซลมีแสง (**ตัวตัดสิน**) | `docs/assets/look/beauty/_grid_lit.md` | 52 เพลต |
| ตัวเลขดิบทุกเลก | `_pixel_reshoot/_compare.json`, `_grid_reshoot.json`, `_grid_lit_reshoot.json` | |
| เครื่องมือ | `scripts/_pixel_{reshoot_plan,reshoot_shoot,reshoot_compare,grid_md_from_json,grid_banner,probe_contact,ladder_doc_check,recover_build}.*` | 8 |

ทุกไฟล์ในสามแถวแรกถูกตรวจว่ามีอยู่จริงทีละ path เทียบกับ manifest 64 ชื่อ ที่ดึงจาก
`git log --format=%B -1 21e28a8` แบบ **ไม่ anchor หัวบรรทัด** (กฎที่ `RECOVERY-2026-08-16.md` §5 ตั้งไว้) → **ขาด 0**

---

## 1. พารามิเตอร์มาจากไหน — ไม่มีการเดาแม้ค่าเดียว

เพลตหาย แต่ **log ที่ยิงเพลตพวกนั้นรอด** (`.gitignore:26` ทำให้ `*.log` ถูก ignore และ
`git clean -fd` ไม่แตะไฟล์ที่ ignore). ทั้งสอง log มีบรรทัดละหนึ่งเพลต พร้อมกล้องและ env เต็ม:

```
[shoot] p1-east-level-fog8-45  cine=52,9,48, 52,9,48, 24,9,18, 1  VOXELFORGE_LOOK_FOG=8,45
[probe] p4-zaxis-level  cine=32,7,58, 32,7,58, 32,9,6, 1
```

`scripts/_pixel_reshoot_plan.py` **แกะ** บรรทัดพวกนี้ออกมาเป็นแผน 60 เพลต แล้ว
`_pixel_reshoot_shoot.ps1` ยิงตามแผน — ไม่มีสคริปต์ไหนเลือกค่า knob เอง

**ทำไมต้องอ่านจาก log ไม่ใช่จากสคริปต์:** `_flamingo_beauty_ladder.ps1` (ตัวที่กู้มาจาก
transcript) นิยามไว้แค่ **11 ขั้น** แต่ manifest มี **13 ใบต่อโพส** — `fill-up` / `fill-down`
ถูกเพิ่มในรอบที่สามซึ่ง edit นั้นไม่เคยถูกบันทึก. ถ้าอ่านจากสคริปต์จะ **หายไป 8 ใบเงียบๆ**
และไม่มีตัวนับไหนร้อง. log มีครบ 52.

env ที่ปักทุกใบเหมือนกัน (จาก `ladder.ps1:43-47` + `probe.cmd:32-36`):
`VOXELFORGE_PLAY=1 NOHUD=1 LOOK_QUALITY=ultra CINE_START=1.0 LOOK_GEN=v3`
และก่อนยิงทุกใบจะ **ลบ knob ทั้ง 5 ตัวทิ้งก่อน** (`_LOOK_FOG/_AMBIENT/_EXPOSURE/_ATMOS/_FILL`)
เพื่อไม่ให้ขั้นหนึ่งรับ env ของขั้นก่อนมา — `_LOOK_FILL` อยู่ในลิสต์นี้ทั้งที่สคริปต์เดิมไม่มี

> **`Hour::NIGHT ev100 = 7.5`** ยืนยันแล้วว่าเป็นค่าใน HEAD (`look.rs:1707`) — **ไม่มีเพลตใบไหน
> ในชุดนี้ยิงด้วย 8.6** และไม่มีใบไหนแตะ `Hour::NIGHT` เลย (rung `ev11.3`/`ev12.0` คือ
> `VOXELFORGE_LOOK_EXPOSURE` ของฉากกลางวัน คนละตัวกับค่าคงที่ของ NIGHT)

---

## 2. ⚠️ HEAD ปัจจุบัน **คอมไพล์ไม่ผ่าน** — และไบนารีที่ใช้ยิงคืออะไรกันแน่

สั่งมาว่า "build exe จาก HEAD ปัจจุบัน". ทำแล้ว ใน worktree สะอาดของตัวเอง
(`_pixel_recover_wt`, detached ที่ `2262f71`, `git status` ว่าง, target dir แยก) — **แล้วมันพัง**:

```
error[E0425]: cannot find function `npc_head_lift` in module `crate::anim`
   --> client\src\cutscene.rs:309:54
error[E0004]: non-exhaustive patterns: `HuskState::Alert`, `HuskState::Reposition`, `HuskState::Search` and 2 more not covered
   --> client\src\anim.rs:2550:11
error: could not compile `voxelforge` (bin "voxelforge") due to 2 previous errors
EXIT=101
```

`cutscene.rs` และ `combat.rs` ที่คอมมิตอยู่ใน HEAD เรียกของที่ `anim.rs` **ที่คอมมิตอยู่ใน HEAD**
ยังไม่มี. ตัว `anim.rs` ที่มีของครบคือฉบับ **ที่ยังไม่คอมมิต** ใน working tree หลัก
(เลน anim/TG-pose — มี `npc_head_lift()` ที่บรรทัด 892 และปิด match ครบ)

**สิ่งที่ผมทำ และเปิดเผยตรงนี้:** ผมก็อป `client/src/anim.rs` จาก working tree หลัก
(md5 `2129b64b77454035189b8282d60fc82f`) ลงไปใน worktree ของผมไฟล์เดียว แล้วบิลด์ผ่าน
ผมไม่ได้เขียน `npc_head_lift()` เองหรือเดาค่าที่มันคืน — นั่นเป็นงานของเลนเจ้าของ

⇒ **ไบนารีที่ยิงเพลตชุดนี้ = `2262f71` + `client/src/anim.rs` ฉบับ working tree**
ไม่ใช่ HEAD เปล่าๆ. HEAD เปล่าๆ ยิงไม่ได้เพราะบิลด์ไม่ผ่าน

| ไบนารี | commit | ขนาด | md5 | เวลา |
|---|---|---:|---|---|
| **ใหม่ (ยิงรอบนี้)** | `2262f71` + anim.rs working-tree | 100,991,488 B | `e850f2c0…` | 2026-08-17 14:22 |
| เดิม (ยิงเพลตที่หาย) | `97cb283` | 100,570,112 B | `f8e3a848…` | 2026-08-16 03:23 |

md5 ของไบนารีเดิมตรงกับที่ `flamingo-beauty-ladder-2026-08-16.md` §0 บันทึกไว้ (`f8e3a848…`)
⇒ ไบนารีที่ยิงเพลตชุดที่หายไป **ยังอยู่บนดิสก์** (มันอยู่ใน `target-pixel/` ซึ่ง ignore) — ข้อนี้สำคัญมากในหัวข้อถัดไป

---

## 3. เพี้ยนไหม — วัดสามเลก ไม่ใช่เลกเดียว

เทียบเฉยๆ ว่า "ใหม่ ≠ เก่า" แล้วสรุปว่าลุคเปลี่ยน **ใช้ไม่ได้**: TAA/SSAO dither พลิกพิกเซล
ระหว่างสองรันของคำสั่งเดิมอยู่แล้ว. เลยวัดสามเลก mask เดียวกัน grader เดียวกัน (`_flamingo_beauty_axes.py`)

| เลก | คืออะไร | max \|Δ\| value span | shadow p5 | chroma span | sky % |
|---|---|---:|---:|---:|---:|
| **1 · noise floor** | ไบนารีใหม่ ยิงเพลตเดิมซ้ำรอบสอง (6 ใบ) | **0.1** | 0.0 | **0.001** | 0.00 |
| **2 · control** | ไบนารี **เดิม** `f8e3a848` ยิงผ่านสูตรใหม่ เทียบ `_grid.json` 08-16 (6 ใบ) | **0.2** | 0.1 | **0.003** | 0.00 |
| **3 · ของจริง** | ไบนารี **ใหม่** เทียบ `_grid.json` 08-16 (44 ใบ) | **5.8** | 0.4 | **0.143** | 0.00 |

**เลก 2 คือใจความ.** ไบนารีเดิมที่รอดอยู่บนดิสก์ ยิงผ่าน plan/shooter ตัวใหม่ ได้ตัวเลข
ห่างจากการวัดของ 08-16 แค่ **0.2 L / 0.003 sat** — เท่ากับพิสูจน์ว่า **สูตรยิงใหม่ reproduce
เพลตที่หายได้** และความต่างในเลก 3 จึงเป็น **ของไบนารี ไม่ใช่ของสูตร**

### ความต่างที่ไบนารีใหม่ทำ (44 เพลต ทุกพิกเซล เทียบ `_grid.json`)

| แกน | min Δ | max Δ | mean Δ | เกิน noise floor |
|---|---:|---:|---:|---:|
| value span | −0.1 | **+5.8** | +3.09 | 43/44 |
| shadow p5 | −0.4 | +0.2 | −0.06 | 40/44 |
| chroma span | **−0.143** | −0.014 | **−0.083** | 44/44 |
| sat p5 | +0.062 | +0.125 | +0.092 | 44/44 |
| sky mask % | +0.000 | +0.000 | 0.000 | 0/44 |

- **span กว้างขึ้นทุกใบ** สูงสุด +5.8 (`p8-abelev-eye-horizon-ev12.0` 88.9→94.7)
- **chroma แคบลงทุกใบ** แย่สุด −0.143 (`p3-east-down-fog5-70` 0.648→0.505) — นี่คือทิศทาง **ผิดทาง** เทียบเป้า ≥0.62
- **sky mask ยังนิ่งที่ 0.00% ทั้ง 44 ใบ** ⇒ ฟ้ายังดำบน HEAD เหมือนเดิมเป๊ะ ปัญหาข้อ 1 ของบันไดเดิมยังไม่ถูกแก้

---

## 4. เทียบกับสิ่งที่เอกสารบันไดเดิม **ประกาศไว้** — คำตัดสินยังยืน

`_grid_lit.md` ของเดิม (ตารางตัวตัดสิน) **กู้ไม่ได้**: มันไม่เคยถูกเขียนลง `_grid.json`
(JSON เก็บเฉพาะ mask ทุกพิกเซล) และไม่มี transcript ไหนถือไฟล์นี้ ⇒ ตัวเลข lit ของ 08-16
เหลืออยู่ที่เดียวคือ **ตารางในเอกสาร** `flamingo-beauty-ladder-2026-08-16.md` §2
`scripts/_pixel_ladder_doc_check.py` จึงแกะตารางนั้นมาเทียบกับ `_grid_lit.md` ที่วัดจากเพลตใหม่

แกน #2 value span (แกนเดียวที่เอกสารพิมพ์ครบ 13 ขั้น × 4 โพส) — 08-16 → 08-17:

| ขั้น | p1 | p2 | p3 | p8 |
|---|---|---|---|---|
| r0 | 100.9 → 105.1 (+4.2) | 101.8 → 105.9 (+4.1) | 99.0 → 103.1 (+4.1) | 108.3 → 111.3 (+3.0) |
| fog8-45 | 127.8 → 128.2 (+0.4) | 128.2 → 128.6 (+0.4) | 126.0 → 126.7 (+0.7) | 130.4 → 130.4 (+0.0) |
| fog6-55 | 121.3 → 122.7 (+1.4) | 121.8 → 123.2 (+1.4) | 119.3 → 120.8 (+1.5) | 125.2 → 125.7 (+0.5) |
| fog5-70 | 115.1 → 117.3 (+2.2) | 115.1 → 117.2 (+2.1) | 113.1 → 115.3 (+2.2) | 119.4 → 120.8 (+1.4) |
| amb1700 | 105.3 → 109.0 (+3.7) | 105.9 → 109.4 (+3.5) | 103.4 → 107.0 (+3.6) | 112.7 → 115.0 (+2.3) |
| amb1400 | 104.0 → 108.0 (+4.0) | 104.9 → 108.4 (+3.5) | 102.0 → 106.0 (+4.0) | 111.4 → 114.1 (+2.7) |
| amb1100 | 102.9 → 106.8 (+3.9) | 103.8 → 107.4 (+3.6) | 100.9 → 105.0 (+4.1) | 110.2 → 112.9 (+2.7) |
| ev11.3 | 86.2 → 91.4 (+5.2) | 87.9 → 93.3 (+5.4) | 84.3 → 89.5 (+5.2) | 89.4 → 94.9 (+5.5) |
| ev12.0 | 81.2 → 85.9 (+4.7) | 83.1 → 88.3 (+5.2) | 79.5 → 85.1 (+5.6) | 82.0 → 86.7 (+4.7) |
| atmos-off | 121.2 → 122.7 (+1.5) | 120.2 → 121.8 (+1.6) | 118.0 → 119.9 (+1.9) | 128.1 → 129.0 (+0.9) |
| win-atmosfog | 133.5 → 133.3 (−0.2) | 133.3 → 133.2 (−0.1) | 130.7 → 130.6 (−0.1) | 137.0 → 136.6 (−0.4) |
| fill-up | 100.9 → 105.0 (+4.1) | 102.0 → 105.8 (+3.8) | 99.0 → 103.1 (+4.1) | 108.2 → 111.2 (+3.0) |
| fill-down | 101.1 → 105.1 (+4.0) | 102.1 → 105.9 (+3.8) | 99.1 → 103.2 (+4.1) | 108.3 → 111.3 (+3.0) |

ขยับมากสุดใบเดียว **5.6 L**

**คำตัดสิน §3 ของเอกสารเดิมยังยืนทุกข้อ** (ทดสอบใหม่บนพิกเซลใหม่ ไม่ใช่สมมติ):

1. **ไม่มีขั้นไหนถึง span 150** — ขั้นที่ดีสุดยังเป็น `win-atmosfog` ที่ **136.6** (0/13 ขั้นถึงเป้า)
2. `win-atmosfog` ยังชนะตัวเลขแต่แพ้ภาพ — chroma ร่วงเหลือ **0.160–0.204** (เป้า ≥0.62)
3. `atmos-off` ยังเป็นขั้นเดียวที่ยืด span ได้ทุกโพสพร้อมกับที่ chroma ยังผ่าน
4. `ev100` ยังทำให้ span แย่ลงทุกโพส
5. `amb` / `fill` ยังไม่ขยับพื้นเงา (≤0.2 L) — ทั้งที่ lever มีชีวิต
6. probe ยัง **admit 0 จาก 8 โพส** ด้วยกฎ sky ≥3% (`_contact.png` ยืนยันด้วยตา)

### 🔻 ข้อเดียวที่ต้องเตือน: margin ของ `atmos-off` บางลง

chroma span ของ `atmos-off` (lit) ตกลงทุกโพส และโพสที่คับที่สุดคือ `p3-east-down`:

| | 08-16 | 08-17 | เป้า | margin เหลือ |
|---|---:|---:|---:|---:|
| p3-east-down · atmos-off | 0.700 | **0.639** | 0.62 | **+0.019** (เดิม +0.080) |

ยังผ่าน แต่เหลือ margin แค่ 0.019 — ถ้าไบนารีขยับอีกรอบในทิศเดิม ขั้นที่เอกสารเดิมชูว่า
"ตัวที่ดีที่สุดที่ยืนได้จริง" จะ **ตก** โดยที่ไม่มีใครแตะ knob เลย ⇒ ต้องเฝ้าค่านี้

---

## 5. เทียบกับ `docs/look-acceptance-rubric.md` — พูดตรงว่ามันไม่ทาบกัน

รูบริควัดด้วยเครื่องคนละตัว (`grade_axes.py` / `grade_look.py` บน
`docs/assets/golden-beauty-shot-ref.png` resample 1024×1024) ส่วนบันไดนี้วัดด้วย
`_flamingo_beauty_axes.py` บน **mood board** crop `514,0,1024,1024`
เอกสารบันไดเดิม §6.2 เขียนไว้เองว่า *"ref ยังเป็น mood board ไม่ใช่ approved frame …
ผมยังไม่ยัดเข้ารูบริค"* — และหลังยิงใหม่ **ยังเป็นแบบนั้นอยู่**

จุดสัมผัสจริงมีจุดเดียว: **พื้น G3 ที่ 8% ของขาว** ซึ่งเป้า shadow 8–11% ของบันไดยืมมา
วัดบนเพลตใหม่: shadow floor **5.7–6.2% ของขาว** ทุกขั้นทุกโพส ⇒ **ยังต่ำกว่าพื้น G3**
เหมือนที่ 08-16 รายงาน (5.6–6.1%) — ไม่ดีขึ้น ไม่แย่ลงอย่างมีนัย

**ผมจะไม่แปลงตัวเลขบันไดเป็นคะแนนรูบริค** เพราะ instrument คนละตัว mask คนละแบบ
และ ref คนละใบ — office scar: *metric pipelines don't travel*

---

## 6. `_grid.md` — กู้ของเดิม ไม่ใช่วัดใหม่

`_grid.md` ถูกลบและไม่มีใน transcript (สคริปต์เขียน ไม่ใช่ Write tool). แต่รอบที่เขียนมัน
เขียน `_grid.json` ในการรัน `_flamingo_beauty_table.py` **ครั้งเดียวกัน** (`--md` กับ `--json`
เขียนติดกันจาก dict `cells` ก้อนเดียว) และ JSON รอด ⇒ ตารางกู้ได้จากตัวมันเอง

`scripts/_pixel_grid_md_from_json.py` **ไม่เขียนตารางขึ้นใหม่** — มัน import สคริปต์เดิมแล้ว
รัน `main()` ตัวจริง โดยสลับแค่สองอย่าง: `axes.grade()` → lookup ใน `_grid.json`,
และ dir ของเพลต → temp dir ที่มีไฟล์ชื่อเดียวกัน 44 ใบ (ให้ glob เจอ). รูปแบบตาราง เป้าหมาย
และตรรกะ PASS/fail เป็นของโค้ดเดิมทั้งหมด

**ตรวจข้าม:** ตารางที่กู้ได้รายงาน shade floor **2.0% / 2.3% / 2.7% / 2.3%** ของขาว
ตรงกับที่เอกสารบันไดเดิม §1b เขียนไว้ว่า *"`_grid.md` จึงรายงาน shade floor 2.0–2.7% ของขาว"*
⇒ ตัวเลขที่กู้มาเป็นของเดิมจริง ไม่ใช่ของที่ผมวัดใหม่

ทุกตารางถูกปั๊ม banner บอกที่มา (`scripts/_pixel_grid_banner.py`) เพราะตอนนี้ในดิสก์
มีตัวเลขสองรุ่นใช้ชื่อไฟล์ชุดเดียวกัน — คนเปิดอ่านต้องแยกออกโดยไม่ต้องถาม

---

## 7. ใบที่ **ไม่ใช่** การกู้ — พูดให้ชัด

| ไฟล์ | สถานะ | เหตุผล |
|---|---|---|
| `probe/_contact.png` | **REMAKE** ไม่ใช่ recovery | ไม่มี generator รอด ไม่มี transcript ไม่มีสคริปต์ไหนใน `scripts/` เอ่ยชื่อมัน ⇒ วิธีประกอบของเดิม **ไม่ทราบ**. ตัวใหม่ทำโดย `_pixel_probe_contact.py` และเขียนบนแผ่นเองว่าเป็น remake |
| `_grid_lit.md` | **ตัวเลขใหม่** ไม่ใช่ของเดิม | ของเดิมกู้ไม่ได้ (ดู §4) |
| `_grid_reshoot.md` | ของใหม่ล้วน | เป็นคู่เทียบตรงๆ ของ `_grid.md` |
| 3 แผ่น `ba-*.png` | ประกอบใหม่จากเพลตใหม่ | `_flamingo_beauty_ba.py` (ตัวที่กู้มา) รันได้ตรงๆ ไม่แก้อะไร |

---

## 8. สิ่งที่งานนี้ **ไม่ได้** พิสูจน์

1. **ไม่ได้พิสูจน์ว่าเพลตใหม่เหมือนเพลตเก่า** — มันไม่เหมือน และ §3 บอกไว้ว่าต่างเท่าไร
   ที่พิสูจน์ได้คือ *สูตรยิงถูก* (เลก 2, ห่าง 0.2 L) และ *ความต่างเป็นของไบนารี*
2. **ไม่ได้ระบุว่าคอมมิตไหนใน `97cb283..2262f71` ทำให้ chroma แคบลง** — `look.rs` เปลี่ยนไป
   223 บรรทัดในช่วงนั้น ยังไม่ได้ bisect และจะไม่เดา
3. **ไม่ได้แตะ HEAD ที่บิลด์ไม่ผ่าน** — ผมก็อป `anim.rs` เข้าไปใน worktree ของผมเท่านั้น
   ไม่ได้แก้ ไม่ได้คอมมิต `anim.rs` ของเลนอื่น (ดู §2)
4. **ไม่ได้ยิงเพลตกลางแจ้งที่มีฟ้าจริง** — ฟ้ายังดำ 0.00% ทุกใบ ข้อ 1 ของ §5 เอกสารเดิมยังค้าง
